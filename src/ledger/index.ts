export interface LedgerSchema {
  // Stub for SQLite schema interface
}

export interface LedgerWriter {
  // Stub for SQLite writer interface
}

export interface LangfuseSink {
  // Stub for Langfuse sink interface
}
